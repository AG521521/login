[phases.setup]
nixPkgs = ["nodejs_18", "python311"]

[phases.install]
cmds = [
  "npm install",
  "python3 -m ensurepip --default-pip",
  "pip3 install -r requirements.txt"
]

[start]
cmd = "node server.js"
