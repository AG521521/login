[phases.setup]
nixPkgs = ["nodejs_18", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "npm install",
  "pip3 install aiohttp aiodns pycares --break-system-packages"
]

[start]
cmd = "node server.js"
